import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App text="https://github.com/Moniii333" />
    <h1>First time using React! :)</h1>
  </React.StrictMode>,
  document.getElementById("root")
);